import React from 'react';
import { Box, Typography } from '@mui/material';

const ExportView = () => {
  return (
    <Box p={3}>
      <Typography variant="h5">
        Export Data
      </Typography>
      {/* Add your export UI here */}
    </Box>
  );
};

export default ExportView; 